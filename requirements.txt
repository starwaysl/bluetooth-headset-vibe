# Bluetooth Headset Vibe — Python 依赖
# 安装：pip install -r requirements.txt

# 键盘监听 + 模拟（跨平台，支持蓝牙 HID 键盘）
pynput>=1.7.0

# 配置文件
PyYAML>=6.0

# HTTP 请求（调 AI API）
requests>=2.28.0

# 剪贴板
pyperclip>=1.8.0
