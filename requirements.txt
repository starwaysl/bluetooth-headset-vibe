# Bluetooth Headset Vibe — Python 依赖
# 安装：pip install -r requirements.txt

# macOS 系统事件（监听 + 模拟按键）
pyobjc-core>=10.0
pyobjc-framework-Quartz>=10.0
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-ApplicationServices>=10.0

# 配置文件
PyYAML>=6.0

# HTTP 请求（调 AI API）
requests>=2.28.0

# 剪贴板
pyperclip>=1.8.0
